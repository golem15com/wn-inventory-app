# Composer ignores
vendor
composer.phar
composer.lock

# Framework ignores
.env
.env.*.php
.env.php

# Self-host secrets (Phase 11) — keep the example, ignore the real one
.env.docker
!.env.docker.example
selenium.php
/bootstrap/compiled.php
.phpunit.result.cache

# Hosting ignores
php_errors.log
nginx-error.log
nginx-access.log
nginx-ssl.access.log
nginx-ssl.error.log
sftp-config.json
.ftpconfig

# Editor ignores
nbproject
.idea
.vscode
!.devcontainer/.vscode
_ide_helper.php

# Other ignores
.DS_Store
package-lock.json
.phpunit.cache
node_modules

# Ignore generated public directory from `winter:mirror public`
public
temp

# Typesense container runtime data store (run-typesense.sh DATA_DIR)
/typesense-data

# Phase 15 (HIST-04): .planning/ relocated to its own private repo (D-06).
# Standalone git repo in the working tree, never a submodule, never published.
.planning/
